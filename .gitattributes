# ここに置くものは**すべて署名の対象**。1バイトでも変わると
# 署名検証もSHA-256も通らなくなり、利用者は更新できなくなる。
# したがって git に一切の変換をさせない。
#
# manifest.json も対象。改行を変換されると manifest.sig と合わなくなる
# （push時に「LF will be replaced by CRLF」の警告が実際に出た。
#   このときは blob 側は無傷だったが、環境によっては壊れうる）。
* -text

*.upd  binary
*.sig  binary
